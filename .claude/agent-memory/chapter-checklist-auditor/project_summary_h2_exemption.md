---
name: summary-h2-exemption
description: "What we have so far" or equivalent wrap-up h2 sections are pure-summary sections with no demo; still flag, but note the nature
metadata:
  type: project
---

Ch.24 has a 7th h2 titled "What we have so far" (L947) that contains only a summary paragraph and the Pullout quote, then transitions directly into CaseStudies. It introduces no new quantity and has no embedded demo. This pattern — a summary/recapitulation h2 — appears to be intentional chapter architecture, not an oversight. Still flag it as "no embedded demo" per checklist rule 2, but note that it is a pure-summary section that introduces no new physics and no formula.

**Why:** The auditor's job is to report the gap; the orchestrator decides whether a summary h2 is exempt. Don't suppress the finding, but do add the context note so the orchestrator can make an informed call.

**How to apply:** When a final h2 is clearly a wrap-up (title says "What we have so far", "Summary", "Putting it together", etc.) and has no Formula block, flag demo absence but add "pure-summary section, no new quantity introduced."
