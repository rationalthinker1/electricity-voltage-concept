Ch.12 Circuits & AC: added `/labs/ac-impedance` as lab 3.5 in the `ch3` Conduction bucket, anchored on complex impedance `Z = R + jX` with `X = omega L - 1/(omega C)`. The lab canvas shows the impedance phasor triangle and live voltage/current waveforms, with sliders for R, L, C, frequency, and RMS voltage. Repointed Ch.12 orphan demos `KirchhoffsLaws`, `MultimeterProbe`, `Impedance`, `ThreePhase`, `TheveninEquivalent`, `VoltageDivider`, `PowerFactor`, and `Superposition` to `deeperLab={{ slug: 'ac-impedance', label: 'See full lab' }}`. Existing `RCTransient`, `LCOscillation`, and `RLCResonance` links were already covered and were left unchanged.
