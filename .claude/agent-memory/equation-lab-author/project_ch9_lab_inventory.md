Ch.9 EM Waves: added `/labs/em-waves` as lab 4.5 in the `ch4` Energy & Fields bucket, anchored on `v = 1 / sqrt(mu epsilon) = c / sqrt(mu_r epsilon_r)`. It covers Maxwell's wave-speed derivation, wavelength/frequency, E/B amplitude relation, intensity, radiation pressure, interference, dB power ratios, and near-field failure of the plane-wave assumption. Repointed the six Ch.9 orphan demos `EMSpectrum`, `Polarization`, `PolarizationMalusLaw`, `RadiationPressure`, `SpeedOfLight`, and `WireToAntennaTransition3D` to `deeperLab={{ slug: 'em-waves', label: 'See full lab' }}`. `OscillatingDipole` and `PlaneWave` already point to `/labs/poynting` and were left unchanged.
